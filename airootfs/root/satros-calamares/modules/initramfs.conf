---

# Arch uses mkinitcpio; Calamares' generic initramfs module calls the
# Debian-specific update-initramfs command and cannot be used here.
dontChroot: false
timeout: 600
script:
    - command: "/usr/local/bin/satros-generate-initramfs"
