# The live root account uses zsh by default; this file is kept as a safe
# fallback for shells that read .profile.  Do not start X here as well as in
# .bash_profile/.zprofile, otherwise a login shell can create a startx loop.
